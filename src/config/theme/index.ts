import { Dimensions } from 'react-native';

const ICON_SIZE = 50;
const ITEM_WIDTH = 250;
const SPACING = 10;
const { width } = Dimensions.get('window');

export { ICON_SIZE, ITEM_WIDTH, width, SPACING };
